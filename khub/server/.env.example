# Example environment variables for production
# Copy this to `.env` for local testing (DO NOT commit secrets)

# MongoDB connection string (replace with your DB and encoded password)
MONGODB_URI="mongodb+srv://kaungsethmue_2001:YOUR_PASSWORD@cluster0.p61y84z.mongodb.net/yourDatabase?retryWrites=true&w=majority"

# JWT secret used to sign tokens (generate a long random string)
JWT_SECRET="replace-with-generated-secret"

# Frontend API URL (used if building frontend on the server or locally)
VITE_API_URL="https://api.your-domain.z.com"

# Other optional settings
NODE_ENV=production
CORS_ORIGIN="https://your-frontend-domain"
PORT=4000
JWT_SECRET=super-secret-demo-key
PORT=4000
